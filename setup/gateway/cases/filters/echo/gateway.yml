path.data: data
path.logs: log

entry:
  - name: my_es_entry
    enabled: true
    router: my_router
    max_concurrency: 2000
    network:
      binding: 0.0.0.0:8000

flow:
  - name: test
    filter:
      - echo:
#          message: "hello world"
          message: "$[[_ctx.request.uri]]"
#          repeat: 1
#          stdout: false

router:
  - name: my_router
    default_flow: test
